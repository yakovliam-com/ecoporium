pluginManagement {
    plugins {
        id "com.github.johnrengelman.shadow" version "7.1.2"
        id "java"
    }
}

rootProject.name = 'ecoporium'
include 'api'
include 'plugin'
include 'plugin'

findProject(":api")?.setName("ecoporium-api")

